#include <stdio.h>
/**
 * main - Entry point of Prog
 * Return: Always 0 (Success)
 */

int main(void)
{
	char p;

	int f;

	p = 'a';
	f = 0;
	while
		(f < 10) {
			putchar(f + '0');
			f++;
		}
	while
		(p <= 'f') {
			putchar(p);
			p++;
		}
	putchar('\n');
	return (0);
}
