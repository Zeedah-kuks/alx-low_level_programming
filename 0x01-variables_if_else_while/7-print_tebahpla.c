#include <stdio.h>
/**
 * main - Entry point of Program
 * Return: Always 0 (Success)
 */

int main(void)
{
	char d;

	d = 'z';
	while
		(d >= 'a') {
			putchar(d);
			d--;
		}
	putchar('\n');
	return (0);
}
