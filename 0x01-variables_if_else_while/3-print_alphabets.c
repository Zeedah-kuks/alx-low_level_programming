#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c;

	char y;

	c = 'a';
	y = 'A';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	while
		(y <= 'Z') {
			putchar(y);
			y++;
		}
	putchar('\n');
	return(0);
}
