#include "main.h"
/**
 * _abs - calculates the absolute value of an integer
 *
 * @k: the int to be checked
 *
 * Return: the absolute value of int
 */
int _abs(int k)
{
	if (k >= 0)
	{
		return (k);
	}
	return (-k);
}
